<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.swordForum.mapper.ManageMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.swordForum.model.Manage">
        <id column="mid" property="mid"/>
        <result column="mname" property="mname"/>
        <result column="mpassword" property="mpassword"/>
        <result column="msex" property="msex"/>
        <result column="mrole" property="mrole"/>
        <result column="memail" property="memail"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        mid, mname, mpassword, msex, mrole, memail
    </sql>

</mapper>
