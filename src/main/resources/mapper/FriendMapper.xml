<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.swordForum.mapper.FriendMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.swordForum.model.Friend">
        <id column="fid" property="fid"/>
        <result column="fromuid" property="fromuid"/>
        <result column="touid" property="touid"/>
        <result column="time" property="time"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        fid, fromuid, touid, time
    </sql>

</mapper>
